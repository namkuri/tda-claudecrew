{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ClaudeCrew 기본 권한: 폴더 선택(dialog), 링크 열기(opener), 자체 커맨드.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
